<h1><%= @user.name %></h1>
<h3><%= @user.bio %></h3>

<% @user.cups.each do |cup| %>
  <%= link_to "#{cup.keg.name} - #{cup.rating}", keg_path(cup.keg) %>
<% end %>

<%= link_to "Delete User", user_path(@user), method: :delete, data: {confirm: 'Are you sure you want to delete?'} %>
    
